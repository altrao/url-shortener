shortener:
  base-url: http://test-url.com

spring:
  cassandra:
    keyspace-name: url_shortener
    schema-action: CREATE_IF_NOT_EXISTS
    connection:
      connect-timeout: 30s
      init-query-timeout: 30s
